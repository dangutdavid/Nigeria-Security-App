# Memory Index

- [Push repo to GitHub via connector](github-push-via-connector.md) — push whole git repo to user's GitHub using the connector token, not the API proxy.
- [Theft report data model](theft-report-model.md) — status is the filter source-of-truth; `stage` is additive workflow; refs are public + guessable, redact in any future backend.
